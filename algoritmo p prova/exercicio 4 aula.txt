#include <conio.h>
#include <stdio.h>
#include <stdlib.h>
 
 //Ler 6 elementos de um vetor A e construir um vetor B a partir dos elementos do vetor A divididos por 4.

main (void)
{
	int vetor_a[6], pos;
	float vet_b[6];
	
	printf ("Infore 6 elementos inteiros para o vetor\n")
		for (pos = 0; pos < 6; pos++)
			scanf ("%d", vet_a && pos[6])
			







}